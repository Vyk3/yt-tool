# Fixture Generation Provenance

Generated: 2026-06-24

## Reference FFmpeg

```
ffmpeg version 8.1.1 (Homebrew)
Apple clang version 21.0.0 (clang-2100.0.123.102)
macOS arm64
```

## Generation

All fixtures generated by `../generate_fixtures.sh` using the reference
ffmpeg above. Fixtures are NOT bit-reproducible across different ffmpeg
builds or OS versions — the SHA256SUMS file records the committed fixtures.

CI only runs `test_minimal.sh` against the minimal binary; it does not
re-generate fixtures.

## Vorbis Fixture

`audio_vorbis.ogg` generated via `oggenc` (Xiph.Org libVorbis) from a
PCM WAV source, because the Homebrew ffmpeg 8.1.1 build does not include
the libvorbis encoder.
