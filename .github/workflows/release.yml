name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      channel:
        description: 'yt-dlp channel (stable or nightly)'
        required: false
        default: stable
        type: choice
        options: [stable, nightly]

jobs:
  build-macos:
    name: build macOS app
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Build release app
        run: |
          zsh scripts/build/swift/build.sh \
            --release \
            --channel "${{ github.event.inputs.channel || 'stable' }}" \
            --skip-test

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: yt-tool-macOS
          path: |
            swift/dist/YTTool.zip
            swift/dist/YTTool.dmg

  publish:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: yt-tool-macOS
          path: dist/

      - name: Extract release notes from CHANGELOG
        id: notes
        run: |
          TAG="${{ github.ref_name }}"
          python3 scripts/release/extract_release_notes.py "$TAG" --out release_notes.md || {
            echo "⚠️  Version not found in CHANGELOG, using fallback"
            echo "Release $TAG" > release_notes.md
          }

      - name: Ensure release exists
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ github.ref_name }}"
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" --title "$TAG" --notes-file release_notes.md --verify-tag
          else
            gh release edit "$TAG" --notes-file release_notes.md
          fi

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ github.ref_name }}"
          gh release upload "$TAG" dist/YTTool.zip dist/YTTool.dmg --clobber
