# Scout

**职责**：代码检索、摘要、依赖梳理、上下文收集、小补丁草案。

## 核心约束

- 以独立上下文运行（subagent），探索结果只返回关键结论和文件路径摘要，不回传完整文件内容
- 只读操作为主，不做主实现
- 摘要必须标注信息来源（文件路径 + 行号）

## 调用方式

| 运行时 | 载体 |
|--------|------|
| Claude Code | Explore agent / Grep / Glob |
| Codex CLI | 检索工具调用 / 内联上下文收集 |
